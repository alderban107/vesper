# Server CI — Elixir tests with PostgreSQL
#
# Runs mix test when server/ files change. Uses a gate job
# (server-checks) so branch protection gets a green check even
# when the workflow skips because only non-server files changed.
#
# Change detection diffs the branch against its merge-base with
# main, so every push is evaluated against the full set of changes
# the branch introduces — not just the latest commit.

name: Server Tests

on:
  push:
    branches-ignore: [main]
    tags-ignore: ["v*"]
  pull_request:
    branches: [main]

concurrency:
  group: test-server-${{ github.ref }}
  cancel-in-progress: true

jobs:
  server-changes:
    runs-on: ubuntu-latest
    outputs:
      server: ${{ steps.check.outputs.server }}
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
        with:
          fetch-depth: 0

      - name: Check for server changes
        id: check
        run: |
          git fetch origin main --depth=1 2>/dev/null || true
          BASE=$(git merge-base origin/main "${{ github.sha }}" 2>/dev/null || echo "")

          if [ -z "$BASE" ]; then
            # No merge-base found (orphan branch, shallow clone, etc.)
            # — run tests to be safe.
            echo "server=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # Any non-markdown files changed under server/ or this workflow?
          if git diff --name-only "$BASE" "${{ github.sha }}" -- 'server/' '.github/workflows/test-server.yml' | grep -qvE '\.md$'; then
            echo "server=true" >> "$GITHUB_OUTPUT"
          else
            echo "server=false" >> "$GITHUB_OUTPUT"
          fi

  server-test:
    needs: server-changes
    if: needs.server-changes.outputs.server == 'true'
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:17@sha256:a426e44bac0b759c95894d68e1a0ac03ecc20b619f498a91aae373bf06d8508d
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      MIX_ENV: test
      TEST_DB_HOST: localhost
      TEST_DB_USER: postgres
      TEST_DB_PASS: postgres

    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - uses: erlef/setup-beam@54075bcc5e249e4758d363f27d099f55d843f124 # v1
        with:
          elixir-version: "1.18"
          otp-version: "27"

      - name: Cache deps
        uses: actions/cache@caa296126883cff596d87d8935842f9db880ef25 # v5
        with:
          path: |
            server/deps
            server/_build
          key: mix-${{ runner.os }}-${{ hashFiles('server/mix.lock') }}
          restore-keys: mix-${{ runner.os }}-

      - name: Install dependencies
        working-directory: server
        run: mix deps.get

      - name: Audit retired packages and security advisories
        working-directory: server
        run: |
          mix hex.audit
          mix deps.audit

      - name: Compile (warnings as errors)
        working-directory: server
        run: mix compile --warnings-as-errors

      - name: Setup database
        working-directory: server
        run: |
          mix ecto.create --quiet
          mix ecto.migrate --quiet

      - name: Verify history migration upgrade path
        working-directory: server
        run: ./scripts/verify-history-upgrade.sh

      - name: Run tests
        working-directory: server
        run: mix test

  # Gate job — branch protection should require this check.
  # Succeeds when tests pass OR when tests were skipped (no server changes).
  # Fails only when tests actually fail.
  server-checks:
    if: always()
    needs: [server-changes, server-test]
    runs-on: ubuntu-latest
    steps:
      - name: Evaluate result
        run: |
          if [ "${{ needs.server-test.result }}" = "failure" ] || [ "${{ needs.server-test.result }}" = "cancelled" ]; then
            echo "Server tests failed or were cancelled"
            exit 1
          fi
          echo "Server tests passed or were skipped (no server changes)"
